test_app:
  build:
    image: dkcodeship/test-dockercfg-generator
    dockerfile_path: Dockerfile

test_dockercfg_generator:
  image: codeship/dockercfg-generator
  add_docker: true
  encrypted_env_file: credentials.env.encrypted
